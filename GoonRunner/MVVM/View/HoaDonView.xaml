<UserControl x:Class="GoonRunner.MVVM.View.HoaDonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodel="clr-namespace:GoonRunner.MVVM.ViewModel"
             d:DataContext="{d:DesignInstance Type=viewmodel:HoaDonViewModel}"
             DataContext="{StaticResource HoaDonVM}"
             mc:Ignorable="d"
             d:DesignWidth="590"
             d:DesignHeight="700" >
    <UserControl.Resources>
        <SolidColorBrush x:Key="Foreground" Color="#3c3836"/>
        <SolidColorBrush x:Key="Foreground2" Color="#A89984"/>
        <SolidColorBrush x:Key="Background" Color="#fbf1c7"/>
        <SolidColorBrush x:Key="Orange" Color="#D65D0E"/>
        <SolidColorBrush x:Key="Green" Color="#427B55"/>

    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="1" FontSize="20"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Foreground="{StaticResource Foreground}" 
                       FontFamily="../../Assets/Fonts/#JetBrains Mono"
                       FontWeight="Medium" >
                        Tất Cả Hóa Đơn
            </TextBlock>
            <StackPanel Grid.Column="2" Margin="0,0,20, 0"
                        Orientation="Horizontal"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Right">
                <Button Style="{StaticResource NavButtonChangeColor}" >
                    <ui:SymbolIcon Symbol="ArrowSort16" FontSize="20"/>
                </Button>
                <Button Style="{StaticResource NavButtonChangeColor}"
                        Command="{Binding RefreshCommand}">
                    <ui:SymbolIcon Symbol="ArrowClockwise24" FontSize="20"/>
                </Button>
            </StackPanel>
        </Grid>

        <ListView Grid.Row="1"
                  Name="ListViewPNH"
                  Margin="5,0,10,0"
                  FontFamily="../../Assets/Fonts/#JetBrains Mono" 
                  FontWeight="Medium"
                  ItemsSource="{Binding HoaDonList}"
                  SelectedItem="{Binding SelectedItem,Mode=OneWayToSource}"
                  VerticalAlignment="Top"
                  BorderBrush="{StaticResource Foreground}"
                  Background="Transparent" >
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Mã HD" DisplayMemberBinding="{Binding MaHD}" />
                    <GridViewColumn Header="Mã KH" DisplayMemberBinding="{Binding MaKH}"/>
                    <GridViewColumn Header="Họ tên KH" DisplayMemberBinding="{Binding TenKH}"/>
                    <GridViewColumn Header="SDT KH" DisplayMemberBinding="{Binding SdtKH}"/>
                    <GridViewColumn Header="Địa chỉ KH" DisplayMemberBinding="{Binding DiaChi}"/>
                    <GridViewColumn Header="Mã NV" DisplayMemberBinding="{Binding MaNV}"/>
                    <GridViewColumn Header="Họ tên NV" DisplayMemberBinding="{Binding TenNV}"/>
                    <GridViewColumn Header="Ngày mua hàng" DisplayMemberBinding="{Binding NgayMuaHang}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>