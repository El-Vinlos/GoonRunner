<UserControl x:Class="GoonRunner.MVVM.View.ChiTietHoaDonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodel="clr-namespace:GoonRunner.MVVM.ViewModel"
             xmlns:gr="clr-namespace:GoonRunner.CustomControl"
             d:DataContext="{d:DesignInstance Type=viewmodel:ChiTietHoaDonViewModel}"
             DataContext="{StaticResource ChiTietHoaDonVM}"
             mc:Ignorable="d"
             d:DesignWidth="590"
             d:DesignHeight="700" >
    <UserControl.Resources>
        <SolidColorBrush x:Key="Foreground" Color="#3c3836"/>
        <SolidColorBrush x:Key="Foreground2" Color="#A89984"/>
        <SolidColorBrush x:Key="Background" Color="#fbf1c7"/>
        <SolidColorBrush x:Key="Orange" Color="#D65D0E"/>
        <SolidColorBrush x:Key="Green" Color="#427B55"/>

    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="1" FontSize="20"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Foreground="{StaticResource Foreground}" 
                       FontFamily="../../Assets/Fonts/#JetBrains Mono"
                       FontWeight="Medium" >
                        Chi Tiết Phiếu Nhập
            </TextBlock>
            <StackPanel Grid.Column="2" Margin="0,0,20, 0"
                        Orientation="Horizontal"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Right">
                <Button Style="{StaticResource NavButtonChangeColor}" >
                    <ui:SymbolIcon Symbol="ArrowSort16" FontSize="20"/>
                </Button>
                <Button Style="{StaticResource NavButtonChangeColor}"
                        Command="{Binding RefreshCommand}">
                    <ui:SymbolIcon Symbol="ArrowClockwise24" FontSize="20"/>
                </Button>
            </StackPanel>
        </Grid>

        <gr:Textbox Grid.Row="1" 
                    x:Name="TextboxMaPNH"
                    Margin="5,0,4,10"
                    SetHeader="Mã hóa đơn"
                    SetPlaceholder="Nhập Mã hóa đơn"
                    SetBinding="{Binding MaHD, Mode=TwoWay}"/>

        <ListView Grid.Row="2"
                  Name="ListViewHoaDon"
                  Margin="5,0,10,0"
                  FontFamily="../../Assets/Fonts/#JetBrains Mono" 
                  FontWeight="Medium"
                  ItemsSource="{Binding ChiTietHoaDonList}"
                  SelectedItem="{Binding SelectedItem,Mode=OneWayToSource}"
                  VerticalAlignment="Top"
                  BorderBrush="{StaticResource Foreground}"
                  Background="Transparent" >
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Mã SP" DisplayMemberBinding="{Binding MaSP}" />
                    <GridViewColumn Header="Tên SP" DisplayMemberBinding="{Binding TenSP}"/>
                    <GridViewColumn Header="Số lượng bán" DisplayMemberBinding="{Binding SoLuongDat}"/>
                    <GridViewColumn Header="Đơn giá" DisplayMemberBinding="{Binding DonGia}"/>
                    <GridViewColumn Header="Thành tiền" DisplayMemberBinding="{Binding ThanhTien}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
